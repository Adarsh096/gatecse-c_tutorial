## C language prep notes

### Building The Project:
From the root directory of the project where makefile is present:

`$ sudo make`

### Running the compiled app :
From the root directory of project :

`$ ./myapp`

### Cleanup 
From the root of the project :

`$ sudo make clean`